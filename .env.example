# Database (leave empty for SQLite fallback to dev.db)
DATABASE_URL=postgres://fx_user:fx_password@localhost:5432/fx_trading

# Server
PORT=3000

# JWT (change in production)
JWT_SECRET=your-secret-key-change-this

# Email (optional - configure to enable OTP emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# FX Rate API
FX_API_URL=https://open.er-api.com/v6/latest
FX_BASE=NGN

# Redis (leave empty to use in-memory fallback)
REDIS_URL=redis://127.0.0.1:6379

# Database (local SQLite)
SQLITE_DB=dev.db
